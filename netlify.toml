[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "#custom"
  command = "npm run dev"
  targetPort = 5173
  port = 8888
  publish = "dist"

# This part is critical: it tells the browser where to find your functions
[functions]
  directory = "netlify/functions"
